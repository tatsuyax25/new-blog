<%- include('layouts/main', { title: 'Dashboard', user: user }) %>
<h1>Dashboard</h1>
<% if (blogs.length > 0) { %>
  <ul>
    <% blogs.forEach(blog=> { %>
      <li>
        <%= blog.title %>
      </li>
      <% }) %>
  </ul>
<% } else { %>
  <p>No blogs found.</p>
<% } %>